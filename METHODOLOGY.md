# 风格排名方法说明

## 测试设计：两两比较 (Pairwise Comparison)

32 个照片风格滤镜，每次给受访者展示**同一张照片的两个风格版本**，问「你更喜欢哪个」。

选择两两比较而非打分制（1-5 分）的原因：
- 判断简单、门槛低，不容易出现「全都给 3 分」的无效数据
- 相对偏好比绝对评分更可靠

## 配对策略：平衡轮转

每个受访者每次做 20 对。配对不是完全随机的——算法确保每个风格与其他每个风格**对阵次数大致均衡**，避免某个风格只跟弱手打、另一个只跟强手打。

从实际数据看，每个风格的 PK 数在 235-255 左右，分配比较均匀。总数据量约 2763 条对比记录。

## 排名算法：Bradley-Terry 模型

### 为什么不用胜率排？

一句话：**不是所有对手都一样强。**

试想一个国际象棋类比：
- 业余棋手跟街头路人下了 100 盘，赢了 90 盘（胜率 90%）
- 职业棋手跟世界冠军们下了 100 盘，赢了 50 盘（胜率 50%）

按胜率排，业余棋手第一——这显然不对。真正的排名必须考虑**对手实力**。

风格排名同样如此。实际数据中出现了胜率 53% 却排第 5、胜率 46% 却排第 4 的情况——因为后者更多对上强敌，赢的每一场含金量更高。

### Bradley-Terry 模型原理

BT 模型给每个风格估算一个**潜在强度值 π**（类似于 Elo 分）。基本假设：

> 风格 i 赢风格 j 的概率 = π_i / (π_i + π_j)

例如 π_i = 4, π_j = 1，则 i 赢 j 的概率是 4/(4+1) = 80%。

### 迭代计算

给定所有对战结果，BT 通过迭代法反推 π 值：

```
π_i = W_i / Σ [ n_ij / (π_i + π_j) ]
```

- **分子 W_i**：该风格的总胜场数
- **分母**：该风格与每个对手 j 的对阵次数 n_ij，除以当前估算的强度之和
- 用所有 2763 次对比数据，迭代至数值收敛

每个风格的 π 值初始设为 1.0，通常几十轮迭代后收敛。最终按 π 值从大到小排列即得排名。

### 核心思想

这不是投票，这是**体育联赛积分**。赢一个强敌比赢一个弱敌加更多分。BT 模型纠正了「虐菜刷胜率」的偏差，所以胜率高的不一定排前面——要看赢的是谁。

## 各项指标说明

| 指标 | 含义 |
|------|------|
| **排名** | 按 BT 强度 π 从大到小排列（已考虑对手实力） |
| **胜率** | 该风格在所有 PK 中获胜的比例（平局不计入） |
| **±6%** | 95% 置信区间的误差范围。真值有 95% 概率落在「胜率 ±6%」区间。PK 数约 250 次时统计误差约为 ±6% |
| **PK** | 该风格参与的有效对比总次数 |

## 置信区间与显著性

样本量约 250 次 PK 时，胜率的抽样误差约 ±6%（95% 置信）。这意味着：
- 两个风格的胜率差小于约 12% 时，不能 100% 确定谁更好
- BT 强度值比单纯胜率更可靠，因为它综合了对手信息
- 相邻排名之间的差异可能不够显著——但 **TOP 3 与后半段之间的差距是非常确定的**

## 数据去重

同一受访者、同一对风格，只计算一次（防止误双击导致重复提交）。

## 参考

- Bradley, R. A., & Terry, M. E. (1952). Rank analysis of incomplete block designs: I. The method of paired comparisons. *Biometrika*, 39(3/4), 324-345.
- Elo, A. E. (1978). *The Rating of Chessplayers, Past and Present*. Arco Publishing.
